# ===== Grafana 配置 =====
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# ===== 主数据中心 vCenter 配置 =====
VSPHERE_DC1_HOST=vcenter-dc1.example.com
VSPHERE_DC1_USER=monitoring-dc1@vsphere.local
VSPHERE_DC1_PASSWORD=your-dc1-password

# ===== 灾备数据中心 vCenter 配置 =====
VSPHERE_DC2_HOST=vcenter-dc2.example.com
VSPHERE_DC2_USER=monitoring-dc2@vsphere.local
VSPHERE_DC2_PASSWORD=your-dc2-password

# ===== 分支机构 vCenter 配置 =====
VSPHERE_BRANCH_HOST=vcenter-branch.example.com
VSPHERE_BRANCH_USER=monitoring-branch@vsphere.local
VSPHERE_BRANCH_PASSWORD=your-branch-password

# ===== 开发测试环境 vCenter 配置 =====
VSPHERE_DEV_HOST=vcenter-dev.example.com
VSPHERE_DEV_USER=monitoring-dev@vsphere.local
VSPHERE_DEV_PASSWORD=your-dev-password

# ===== VMware 通用配置 =====
VSPHERE_IGNORE_SSL=True

# ===== 说明 =====
# 1. 每个 vCenter 使用独立的监控账号
# 2. 建议在每个 vCenter 创建只读用户，权限要求:
#    - 全局权限: 只读
#    - 对象权限: 数据中心、主机、虚拟机、数据存储
# 3. 如果使用域账号，格式: username@domain
# 4. 如果使用本地账号，格式: username
# 5. VSPHERE_IGNORE_SSL=True 用于自签名证书

# ===== VMware 用户权限配置指南 =====
#
# 创建自定义角色 "Monitoring-ReadOnly":
# 1. 登录 vCenter
# 2. 菜单 -> 管理 -> 访问控制 -> 角色
# 3. 创建新角色，添加以下权限:
#    - Global.Health
#    - Global.Licenses (视图)
#    - Datastore.Browse
#    - Host.Config.SystemManagement (只读)
#    - VirtualMachine.Config.Settings (只读)
#    - VirtualMachine.State.* (所有状态查看权限)
#    - Resource.AssignVMToPool (只读)
#
# 分配权限:
# 1. 选择数据中心根对象
# 2. 权限 -> 添加权限
# 3. 用户: monitoring@vsphere.local
# 4. 角色: Monitoring-ReadOnly
# 5. 勾选"传播到子对象"

# ===== 多租户监控场景 =====
# 如果需要为不同部门/租户创建独立的监控用户:
#
# 租户 A (财务部):
# VSPHERE_TENANT_A_HOST=vcenter.example.com
# VSPHERE_TENANT_A_USER=monitoring-finance@vsphere.local
# VSPHERE_TENANT_A_PASSWORD=password-a
#
# 租户 B (研发部):
# VSPHERE_TENANT_B_HOST=vcenter.example.com
# VSPHERE_TENANT_B_USER=monitoring-dev@vsphere.local
# VSPHERE_TENANT_B_PASSWORD=password-b
#
# 然后在 vCenter 中为每个用户设置不同的权限范围（文件夹/资源池）
